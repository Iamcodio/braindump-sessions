#!/usr/bin/env bash
# braindump-start — Launch braindump discord bot in tmux
# Uses discord.py directly, NOT the Anthropic Discord plugin
set -euo pipefail

SESSION="braindump"
BD_DIR="$HOME/09-personal/BrainDumpSessions"

export PATH="$HOME/.local/bin:$HOME/.nvm/versions/node/$(ls "$HOME/.nvm/versions/node/" 2>/dev/null | tail -1)/bin:/opt/homebrew/bin:/usr/local/bin:$PATH"

tmux kill-session -t "$SESSION" 2>/dev/null || true

TMUX= tmux new-session -d -s "$SESSION" \
  "cd $BD_DIR/bot && uv run python braindump_bot.py; exec bash"

echo "Braindump bot started in tmux '$SESSION'"
echo "Attach: tmux attach -t $SESSION"
